import Typography from 'typography'

const typography = new Typography({
  baseFontSize: '21px',
  scaleRatio: 4,
  headerFontFamily: ['bebasneuebold', 'Helvetica', 'sans-serif'],
  headerGray: 80,
  bodyGray: 80,
})

export default typography
